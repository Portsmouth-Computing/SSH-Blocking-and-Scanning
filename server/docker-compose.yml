version: '3'
services:
  web:
    build: .
    ports:
      - "880:80"
    volumes:
      - .:/ssh_blocker
  postgres:
    image: "postgres:10-alpine"
    volumes:
      - ./schema.sql:/docker-entrypoint-initdb.d/schema.sql
